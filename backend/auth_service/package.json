{
  "name": "auth_service",
  "version": "1.0.0",
  "description": "Authentication Service for project ShipLink",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon -r dotenv/config src/index.ts",
    "start": "tsc -p . && node dist/src/index.js",
    "test": "jest",
    "format": "prettier --write . --log-level silent",
    "build": "tsc -p ."
  },
  "author": "KushalSuvan",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "prettier": "^3.3.3",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.693.0",
    "@aws-sdk/lib-dynamodb": "^3.693.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
